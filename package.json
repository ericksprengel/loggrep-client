{
  "name": "test-loggrep-client",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "engines": {
    "node": ">=8.0.0"
  },
  "dependencies": {
    "chalk": "^4.1.2",
    "loggrep": "^2.1.0"
  },
  "devDependencies": {
    "@types/node": "^17.0.4",
    "typescript": "^4.5.4"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -b",
    "loggrep": "yarn build && loggrep"
  }
}
